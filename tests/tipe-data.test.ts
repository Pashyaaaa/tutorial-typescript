describe("Tipe Data", () => {
  it("should must declare", () => {
    const name: string | number = "Pashya";
    const age: number = 20;
    const isMarried: boolean = false;

    console.info(name);
    console.info(age);
    console.info(isMarried);
  });
});
